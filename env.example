# HubSpot App Configuration
# Get these values from your HubSpot app settings:
# https://app.hubspot.com/developers/{your-account-id}/applications

CLIENT_ID=your-hubspot-client-id-here
CLIENT_SECRET=your-hubspot-client-secret-here

# OAuth Scopes
# Enter as a SINGLE STRING - space-separated OR comma-separated
# IMPORTANT: 'oauth' and 'files' are INVALID and will be automatically removed
# Use specific scopes like: files.read, files.write, files.ui_hidden.read
# 
# Space-separated (recommended):
SCOPE=crm.objects.contacts.read crm.objects.contacts.write crm.objects.custom.read crm.objects.custom.write files.ui_hidden.read
#
# Comma-separated also works:
# SCOPE=crm.objects.contacts.read,crm.objects.contacts.write,crm.objects.custom.read,crm.objects.custom.write,files.ui_hidden.read

# Redirect URI (automatically set by Netlify, but you can override)
# This should match the redirect URI configured in your HubSpot app
# Format: https://your-site-name.netlify.app/oauth-callback
# REDIRECT_URI=https://your-site-name.netlify.app/oauth-callback
