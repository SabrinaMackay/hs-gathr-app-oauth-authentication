{
  "name": "hubspot-oauth-netlify",
  "version": "1.0.0",
  "description": "HubSpot OAuth 2.0 token exchange server deployed on Netlify",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "keywords": [
    "hubspot",
    "oauth",
    "netlify"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^2.6.7",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
